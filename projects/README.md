# Projects

Create real research projects here with:

```bash
python scripts/new_project.py <project_id>
```

Recommended project id format:

```text
YYYY-MM-short-topic
```

Example:

```text
2026-04-graph-cache-optimization
```

Each project is an audit directory. The manuscript is not the source of truth; evidence and claim traces are.

